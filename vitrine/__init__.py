"""Vitrine — local, reproducible 3D Gaussian Splatting for digital preservation.

Pipeline: ``ingest`` → ``sfm`` → ``train`` → ``package``.

Runs entirely on the machine in front of you, with no cloud services and no
paid software, on hardware from a 6 GB laptop GPU up to a lab workstation.
See ``AGENTS.md`` for the design rationale and the measurements behind it.
"""

__version__ = "0.1.0"
